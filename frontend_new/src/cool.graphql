query test($first:Int,$last:Int,$before:String,$after:String,$filter:UsersFilterInput,$orderBy:OrderBy,$skip:Int,$back:Boolean,$page:Int) {
  numbers(first: $first, last:$last,before:$before,after:$after,filter:$filter,orderBy:$orderBy, skip:$skip,back:$back,page:$page) {
    __typename
      edges {
        node {
          id
          username
        }
      }
      pageInfo {
        hasPreviousPage
        hasNextPage
        startCursor
        endCursor
      }
      totalCount
  }
}
